{% extends 'base.html.twig' %}
{% block title %}Accueil {% endblock %}
{% block body %}

<section class="row mb-2 center">

{% for produit in produits %}
    <div class="test col-3 mb-3">
	   <div class="card h-100">
	  <img src="{{ asset('images/' ~ produit.image) }}" class="card-img-top " alt="..." width="300px" height="300px">
	  <div class="card-body">
	    <h5 class="card-title">{{ produit.nom }}</h5>
	    <p class="card-text">{{ produit.prix }} € </p>
	    <a href="{{ path('products_details', {id:produit.id} )}}" class="btn btn-primary">Details</a>
	    <a href="{{ path('panier_add', {id:produit.id} )}}" class="btn btn-primary">Acheter au panier</a>
	  </div>
    </div>
    </div>
{% endfor %}
</section>
{% endblock %}
